<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments | XSS Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="mt-5">Comments Section</h1>

        <ul class="list-group my-3">
            <% comments.forEach(function(comment) { %>
                <li class="list-group-item"><%- comment %></li>
            <% }); %>
        </ul>

        <h2>Leave a Comment</h2>
        <form action="/comment" method="POST" class="mb-5">
            <div class="form-group">
                <textarea name="comment" class="form-control" rows="4" placeholder="Write your comment..."></textarea>
            </div>
            <button type="submit" class="btn btn-success mt-3">Submit Comment</button>
        </form>

        <a href="/" class="btn btn-secondary">Back to Home</a>
    </div>
</body>
</html>
