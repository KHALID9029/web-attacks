<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Internal File Access (Path Traversal)</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Access Internal File (Path Traversal)</h1>
        <p>Enter the path of the file you want to access from the system (e.g., /etc/passwd):</p>

        <!-- Form for manual file access -->
        <form method="POST" action="http://localhost:3005/access_file" class="mt-4">
            <div class="mb-3">
                <label for="filePath" class="form-label">File Path:</label>
                <input type="text" name="filePath" class="form-control" placeholder="e.g., /etc/passwd" required>
            </div>
            <button type="submit" class="btn btn-primary">Fetch File</button>
        </form>

        <!-- Display the file content if it exists -->
        <% if (fileContent) { %>
        <div class="mt-4">
            <h3>File Content:</h3>
            <pre><%= fileContent %></pre>
        </div>
        <% } %>

        <a href="/" class="btn btn-secondary mt-3">Back to Home</a>
    </div>
</body>
</html>
