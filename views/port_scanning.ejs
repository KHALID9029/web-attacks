<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Port Scanning</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Port Scanning</h1>
        <p>Enter a target IP or domain to scan common ports.</p>

        <!-- Form to submit target IP or domain for scanning -->
        <form method="POST" action="http://localhost:3005/port_scan" class="mt-4">
            <div class="mb-3">
                <label for="targetIp" class="form-label">Target IP or Domain:</label>
                <input type="text" name="targetIp" class="form-control" id="targetIp" required>
            </div>
            <button type="submit" class="btn btn-primary">Scan Ports</button>
        </form>

        <!-- Display open ports if available -->
        <% if (openPorts) { %>
        <div class="mt-4">
            <h3>Scan Results:</h3>
            <pre><%= openPorts %></pre>
        </div>
        <% } %>

        <a href="/" class="btn btn-secondary mt-3">Back to Home</a>
    </div>
</body>
</html>
