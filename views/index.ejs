<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Web Attacks Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css"> 
</head>
<body>
    <div class="container text-center">
        <img src="/images/fox-mascot.png" alt="Fox Mascot" class="mascot-img"> 
        <p class="lead fox-message">Let's hunt down some web vulnerabilities!</p>

        <!-- <% if (!user) { %>
            <a href="/login" class="btn btn-primary">Login</a>
        <% } else { %>
            <p>You are logged in as <strong><%= user.username %></strong> (<%= user.role %>)</p>
            <a href="/logout" class="btn btn-danger">Logout</a>
        <% } %> -->

        <% if (user) { %>
            <p>You are logged in as <strong><%= user.username %></strong> (<%= user.role %>)</p>
            <a href="/logout" class="btn btn-danger">Logout</a>
            <a href="/change-password" class="btn btn-warning">Change Password</a> <!-- New button -->
        <% } else { %>
            <a href="/login" class="btn btn-primary">Login</a>
        <% } %>
        
    
        <a href="/comments" class="btn btn-primary">Go to Comments Page</a>
    
        <% if (user && user.role === 'admin') { %>
            <a href="/admin" class="btn btn-primary">Go to Admin Panel</a>
        <% } %>
    </div>
</body>
</html>
